void main(
       float3 aPosition,
       uniform float4x4 wvp,
       float4 out vPosition : POSITION,
       float4 out vColor : TEXCOORD0
       )
{
	vPosition = mul(float4(aPosition, 1.f), wvp);
	vColor = (float4(aPosition, 1.f) + float4(1.f, 1.f, 1.f, 1.f)) / 2.0f;
}